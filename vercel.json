{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "regions": ["mad1"],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}